import { RequestForLoanRequest } from './request-for-loan-request';

describe('RequestForLoanRequest', () => {
  it('should create an instance', () => {
    //expect(new RequestForLoanRequest()).toBeTruthy();
  });
});
