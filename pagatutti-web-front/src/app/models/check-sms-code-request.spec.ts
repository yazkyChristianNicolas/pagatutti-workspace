import { CheckSmsCodeRequest } from './check-sms-code-request';

describe('CheckSmsCodeRequest', () => {
  it('should create an instance', () => {
    expect(new CheckSmsCodeRequest()).toBeTruthy();
  });
});
