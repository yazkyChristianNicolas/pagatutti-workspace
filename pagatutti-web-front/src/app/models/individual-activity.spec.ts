import { IndividualActivity } from './individual-activity';

describe('IndividualActivity', () => {
  it('should create an instance', () => {
    expect(new IndividualActivity()).toBeTruthy();
  });
});
