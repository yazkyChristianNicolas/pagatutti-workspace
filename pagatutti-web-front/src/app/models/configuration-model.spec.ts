import { ConfigurationModel } from './configuration-model';

describe('ConfigurationModel', () => {
  it('should create an instance', () => {
    expect(new ConfigurationModel()).toBeTruthy();
  });
});
