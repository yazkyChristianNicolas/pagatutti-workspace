import { CheckIdOpportunity } from './check-id-opportunity';

describe('CheckIdOpportunity', () => {
  it('should create an instance', () => {
    expect(new CheckIdOpportunity()).toBeTruthy();
  });
});
